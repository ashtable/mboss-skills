{
  "name": "@mboss/skills",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The mboss Agent Skill — teaches agents to drive the mboss MCP server.",
  "license": "MIT",
  "engines": {
    "node": "24.18"
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src",
    "skills"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "lint": "tsc --noEmit && eslint . && prettier --check ."
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.3",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.66.0",
    "vitest": "^4.1.10"
  }
}
